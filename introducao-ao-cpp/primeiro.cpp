// Incluindo uma biblioteca.
// <iostream>: Biblioteca padrão de entrada (in) e saída (out).
#include <iostream>

// Função principal.
int main () {

    // std = standard (padrão)
    // quebra de linha = \n.
    std::cout << "Hello, World!\n Treino de C++.";

    // O "system("pause")" faz com que o .exe fique parado até que seja apertado alguma tecla.
    system("pause");
    
    // Toda função tem um retorno (return). Como a função tem que retornar um interiro (int), eu afirmo que estou retornando um 0.
    return 0;
}

